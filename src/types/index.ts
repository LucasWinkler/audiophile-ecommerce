export type NavigationLink = {
  name: string;
  href: string;
};

export type Category = {
  name: string;
  href: string;
  thumbnail: string;
};

export type IconProps = {
  className?: string;
};
